# Goal: Add Two Numbers

Write a Python function called `add` that takes two numbers and returns their sum.

The function must:
- Accept two numeric arguments (int or float).
- Return their sum as the same numeric type when both inputs match.
- Handle edge cases like negative numbers and zero correctly.
